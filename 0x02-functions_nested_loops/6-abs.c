#include"main.h"

/**
 * _abs: check description
 * @n: An nput number
 * description: computes the absolute value of an integer
 * return:absolute integer value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n*-1);
}
