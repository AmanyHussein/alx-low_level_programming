#include"main.h"
/**
 * puts2 - Prints out every other char of the string.
 * @str: input string to print.
 */

void puts2(char *str)
{
	int l = 0;

	while (str[l] != '\0')
	{
		_putchar(str[i]);
		i+=2;
	}
}
